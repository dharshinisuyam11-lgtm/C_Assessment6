#include <stdio.h>
int main()
{
    int a, b, i;
    scanf("%d%d", &a, &b);
    i = (a < b) ? a : b;
    while(i > 0)
    {
        if(a % i == 0 && b % i == 0)
        {
            printf("%d", i);
            break;
        }
        i--;
    }
    return 0;
}
